# Talk Space

Talk Space is a TanStack Start application for managing therapy services, appointments, clients, content, and administration.

## Local development

Install dependencies, create a `.env` file from `.env.example`, fill in the Supabase values, and start the development server:

```bash
npm install
npm run dev
```
